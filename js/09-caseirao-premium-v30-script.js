
(()=>{
 const cashbackApi=(action,payload={})=>api('cashback-api',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,payload})});
 const premiumMoney=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
 function premiumBlocks(){
  const wrap=document.querySelector('.wrap'),banner=document.querySelector('#banner');if(!wrap||!banner)return;
  let welcome=document.querySelector('#premiumWelcome');if(!welcome){welcome=document.createElement('section');welcome.id='premiumWelcome';welcome.className='premiumWelcome';welcome.innerHTML='<div><small>EXPERIÊNCIA CASEIRÃO</small><h2>Seu hambúrguer, do seu jeito.</h2><p>Pedido direto para a cozinha, acompanhamento em tempo real e vantagens exclusivas.</p></div><div class="premiumWelcomeBadge">PEDIDO SEGURO<br>PELO SISTEMA</div>';banner.after(welcome)}
  let club=document.querySelector('#cashbackClub');if(!club){club=document.createElement('section');club.id='cashbackClub';club.className='cashbackClub';club.innerHTML='<div class="cashbackIcon">$</div><div><b>Clube Cashback Caseirão</b><span>Ganhe saldo nos pedidos entregues e use nas próximas compras.</span></div><button type="button" id="openCashback">VER MEU SALDO</button>';welcome.after(club);club.querySelector('button').onclick=openCashback}
  const picks=(data.products||[]).filter(p=>p.active!==false&&!p.sold_out&&p.image_url).sort((a,b)=>(Number(b.sold_qty)||0)-(Number(a.sold_qty)||0)).slice(0,8);
  let section=document.querySelector('#featuredSection');if(!picks.length){section?.remove();return}if(!section){section=document.createElement('section');section.id='featuredSection';section.className='featuredSection';club.after(section)}
  section.innerHTML='<div class="featuredHead"><h3>Mais pedidos</h3><span>Arraste para escolher</span></div><div class="featuredRail">'+picks.map(p=>`<article class="featuredItem" data-premium-product="${esc(p.id)}"><span class="featuredTag">${Number(p.promo_price)>0?'OFERTA':'DESTAQUE'}</span><img src="${esc(p.image_url)}" alt="${esc(p.name)}"><div class="featuredItemInfo"><b>${esc(p.name)}</b><span>${fmt(priceOf(p))}</span></div></article>`).join('')+'</div>';
  section.querySelectorAll('[data-premium-product]').forEach(el=>el.onclick=()=>openProduct(el.dataset.premiumProduct));
 }
 function openCashback(){modal('<div class="sheeth"><div><h2>Meu cashback</h2><div class="adminSub">Consulte pelo WhatsApp usado nos pedidos</div></div><button class="x" data-close>×</button></div><div class="field"><label>Seu WhatsApp</label><input id="cashbackPhone" class="in" inputmode="tel" placeholder="(86) 99999-9999"></div><button id="cashbackLookup" class="primary">CONSULTAR SALDO</button><div id="cashbackLookupResult"></div>');bindClose();const remembered=JSON.parse(localStorage.getItem('caseirao_customer')||'{}');if(remembered.phone)$('#cashbackPhone').value=remembered.phone;$('#cashbackLookup').onclick=async()=>{const button=$('#cashbackLookup'),box=$('#cashbackLookupResult');try{button.disabled=true;button.textContent='CONSULTANDO...';const r=await cashbackApi('status',{phone:$('#cashbackPhone').value});box.innerHTML=`<div class="cashbackResult"><span>Saldo disponível de ${esc(r.customer.name||'cliente')}</span><div class="cashbackBalance">${premiumMoney(r.customer.balance)}</div><div class="mini">Cashback de ${Number(r.settings.cashback_percent||0)}% liberado após a entrega.</div><div class="cashbackStats"><div class="cashbackStat"><span>Total ganho</span><b>${premiumMoney(r.customer.earned)}</b></div><div class="cashbackStat"><span>Total utilizado</span><b>${premiumMoney(r.customer.used)}</b></div></div></div>`}catch(e){box.innerHTML=`<div class="err">${esc(e.message||String(e))}</div>`}finally{button.disabled=false;button.textContent='CONSULTAR SALDO'}}}
 const renderCatalogBase=renderCatalog;renderCatalog=function(){const out=renderCatalogBase();premiumBlocks();return out};
 let cashbackAvailable=0,cashbackSettings=null,cashbackUse=0;
 const openCheckoutBase=openCheckout;openCheckout=function(){const out=openCheckoutBase();const sum=$('#checkoutSum');if(!sum||$('#cashbackCheckout'))return out;const box=document.createElement('div');box.id='cashbackCheckout';box.className='cashbackCheckout';box.innerHTML='<div class="cashbackCheckoutTop"><b>Cashback Caseirão</b><button type="button" id="checkCheckoutCashback">CONSULTAR</button></div><div id="cashbackCheckoutState" class="cashbackCheckoutState">Consulte seu saldo para usar nesta compra.</div><div id="cashbackUseRow" class="cashbackUseRow hide"><input id="cashbackAmount" inputmode="decimal" placeholder="Valor a usar"><label><input id="cashbackUseToggle" type="checkbox"> USAR</label></div>';sum.before(box);$('#checkCheckoutCashback').onclick=async()=>{const state=$('#cashbackCheckoutState');try{const r=await cashbackApi('status',{phone:$('#custPhone').value});cashbackAvailable=Number(r.customer.balance||0);cashbackSettings=r.settings;const max=Math.min(cashbackAvailable,cartSubtotal()*Number(r.settings.cashback_max_redeem_percent||30)/100);state.textContent=cashbackAvailable>=Number(r.settings.cashback_min_redeem||0)?`Saldo ${premiumMoney(cashbackAvailable)} • você pode usar até ${premiumMoney(max)} neste pedido.`:`Saldo ${premiumMoney(cashbackAvailable)} • mínimo para usar: ${premiumMoney(r.settings.cashback_min_redeem)}.`;$('#cashbackUseRow').classList.toggle('hide',cashbackAvailable<Number(r.settings.cashback_min_redeem||0));$('#cashbackAmount').value=max.toFixed(2).replace('.',',')}catch(e){state.textContent=e.message||String(e)}};$('#cashbackUseToggle').onchange=e=>{cashbackUse=e.target.checked?Math.max(0,Number(String($('#cashbackAmount').value).replace(',','.'))||0):0};$('#cashbackAmount').oninput=()=>{if($('#cashbackUseToggle').checked)cashbackUse=Math.max(0,Number(String($('#cashbackAmount').value).replace(',','.'))||0)};return out};
 const apiBase=api;api=async function(slug,opts={}){if(slug==='create-order'&&opts.body&&cashbackUse>0){try{const body=JSON.parse(opts.body);body.cashback_to_use=cashbackUse;opts={...opts,body:JSON.stringify(body)}}catch{}}const response=await apiBase(slug,opts);if(slug==='create-order')cashbackUse=0;return response};
 setTimeout(premiumBlocks,0);
})();
